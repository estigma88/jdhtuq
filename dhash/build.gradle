apply plugin: 'java'

repositories {
    mavenCentral()
}

version = '1.0.3'

dependencies {
    compile project(':communication')
    compile project(':storage-service')
    compile project(':lookup-service')
}
