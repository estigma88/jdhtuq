#logging:
 # level:
  #  org:
   #   springframework: DEBUG
server:
  port: 8080
communication:
  integration:
    default-request-timeout: 3000
    default-replay-timeout: 3000
    instances:
      chord:
        multicast-server-active: true
        ip-multicast: 224.0.0.1
        port-multicast: 2000
